import { NavBar } from '../components/NavBar';
import UpdateFelixForm from '../features/UpdateFelix';

export default function UpdateProjectPage(): JSX.Element {
  return (
    <div>
      <NavBar />
      <UpdateFelixForm />
    </div>
  );
}
